interface:
  display_name: "Ship Council"
  short_description: "Multi-agent delivery loop for Codex"
  default_prompt: "Use $ship-council to plan, build, review, verify, and loop on this software change."
policy:
  allow_implicit_invocation: true
