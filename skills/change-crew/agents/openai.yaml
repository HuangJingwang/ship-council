interface:
  display_name: "Change Crew"
  short_description: "Context-light delivery workflow for Codex"
  default_prompt: "Use $change-crew only as much as this change needs. Start light, add agents and gates only for risk."
policy:
  allow_implicit_invocation: true
